spring:
  application:
    name: demoEmployees

  datasource:
    url: jdbc:postgresql://localhost:5432/employees_management
    username: postgres
    password: root

  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update

  security:
    user:
      name: admin
      password: admin